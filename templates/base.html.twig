<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Social Network{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

        <nav class="fixed flex w-full items-center justify-between bg-blue-400 py-4 mb-20 shadow-md shadow-black/5">
            <div class="flex w-full flex-wrap items-center justify-around px-6">
                
                <div
                class="flex items-center">
                <a
                    class="mt-2 mr-2 flex items-center text-neutral-900 hover:text-neutral-900 lg:mt-0"
                    href="#">
                    The Social Network
                </a>
                
                <ul
                    class="list-style-none mr-auto flex pl-0 mx-6"
                    data-te-navbar-nav-ref>
                    <li>
                    <a
                        class="text-white hover:text-neutral-700 lg:px-2"
                        href="{{ path('app_home') }}"
                        >Accueil</a
                    >
                    </li>
                    <li>
                    {% if app.user %}
                        <a
                            class="text-white hover:text-neutral-700 lg:px-2"
                            href="{{ path('app_member')}}"
                            >Membres</a
                        >
                    {% endif %}
                    
                    </li>
                </ul>
                
                </div>
                                
                <div class="flex items-center">
                
                {% if app.user %}
                
                    <div class="">
                        <a
                        class="flex items-center"
                        href="{{ path('app_member_show', { 'id' : app.user.id }) }}">
                        <img
                            src="{{ app.user.picture }}"
                            class="rounded-full"
                            style="height: 40px; width: 40px"
                            alt="{{ app.user.name }}"
                            />
                        <span
                        class="mx-2 text-white hover:text-neutral-700">
                            {{ app.user.name }}
                        </span>
                        </a>
                    </div>

                {% else %}
                    
                    <a
                    class="text-white hover:text-neutral-700 lg:px-2"
                    href="{{ path('app_login') }}">Connexion</a>
                    <a
                    class="text-white hover:text-neutral-700 lg:px-2"
                    href="{{ path('app_register') }}">Inscription</a>
                
                {% endif %}
                
                
                </div>
                
            </div>
        </nav>

        <div class="max-w-5xl mx-auto pt-32">
        {% block body %}{% endblock %}
        </div>
    </body>

    <footer class="relative h-16 w-full bg-neutral-200 mt-6 py-6 text-center text-neutral-700">
        <div class="bsolute inset-x-0 bottom-0 h-16" >
            The Social Network - {{ 'now' | date('Y') }} - Tous droits réservés
        </div>
    </footer>
</html>
